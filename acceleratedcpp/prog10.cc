#include <iostream>
#include <string>

using std::cin;         using std::cout;
using std::endl;        using std::string;

// to compare with the program in prog9.cc

int main() {
    string s;
    while (cin >> s)
        cout << s << endl;
    return 0;
}