// Chap 2: Var and Basic Types

int main()
{	
	// identifiers
	// must begin with _ or a letter
	// lower and upper case sensitive
	// conventions: var names are lowercase, classes begin with upperc
	// identifiers with multiple words should visually distinguish 
	// each word.
	int index; // not Index or INDEX
	double Sales_item; // class
	int two_words;
	int twoWords; // twowords is difficult to parse visually
	
	// rule of thumb, be consistent with naming variables
	
	return 0;
}